# XRPL GroupPay Demo

This repo contains a scaffolded demo project for an XRPL group payment flow.

## Structure

- `project_structure.yaml`: File manifest used to generate the project tree.
- `xrpl-grouppay-demo/`: Demo app source and static assets.

## Next steps

Fill in the Python modules under `xrpl-grouppay-demo/app/` and update
`xrpl-grouppay-demo/README.md` with setup and usage instructions.
